export enum HBSType {
    timeLine = "timeLine", //  时间轴
    month = "month", // 月历
    todo = "todo", // todoList
    holes = "holes", // 侧边的洞
    back = "back", // 背景网格|点阵
}

export const hbsTypes: HBSType[] = [
    HBSType.timeLine,
    HBSType.month,
    HBSType.todo,
  ];
  